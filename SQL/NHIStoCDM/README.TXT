NHIS to CDM conversion SQL codes.
One SQL code for each CDM table.

_SY.SQL : edited by SY (need further revision)    

질문: 테이블 만드는 중에 일부년도 이후에 생성된 변수의 경우 cast(null as varchar2(2000))as MPRSC_GRANT_NO 이런식으로 처리.
그런데 애초에 공단 데이터를 DB로 넣을 때 저 변수 자체가 없는 채로 넣었으면 에러 안 생기는지?